-- Initialize a variable with a temperature value in Celsius
local temp_c = 35

-- Calculate the Fahrenheit value
local temp_f = (9 / 5) * temp_c + 32

-- Display the result in the console
print("The value " .. temp_c .. " in Fahrenheit is " .. temp_f)
